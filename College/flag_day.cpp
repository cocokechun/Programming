#include <iostream>
#include <algorithm>
#include <stdlib.h>
#include <stdio.h>
#include <map>

using namespace std;

int solve(char cs[], int n);

int solve() {
    int n, m;
    cin >> n >> m;
    
    long long *nums = (long long*)calloc(n, sizeof(long long));
    
    return min_so_far;
}

int main() {
    int result = solve();

    cout<<result<<endl;
    return 0;
}
